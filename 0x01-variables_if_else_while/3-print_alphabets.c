#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
	{
		putchar(low);
	}
	for lowh = 'A'; low <= 'Z'; low++)
	{
		putchar(low);
	}
	putchat(\n);
	return (0);
}
